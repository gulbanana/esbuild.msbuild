{
  "license": "MIT",
  "packageManager": "yarn@3.6.4",
  "dependencies": {
    "esbuild": "0.25.4"
  },
  "scripts": {
    "build": "nuget pack -OutputDirectory ../dist",
    "test": "rm -r $HOME\\\\.nuget\\\\packages\\\\esbuild.msbuild; dotnet publish ../test/*.csproj"
  }
}
